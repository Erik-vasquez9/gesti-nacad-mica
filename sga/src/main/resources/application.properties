spring.datasource.url=jdbc:postgresql://localhost:5432/gestion_academica_db
spring.datasource.username=postgres
spring.datasource.password=Erik21

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Opcional: si quieres que Hibernate “asuma” el schema por defecto
# (igual pondremos schema en cada @Table)
spring.jpa.properties.hibernate.default_schema=academico
